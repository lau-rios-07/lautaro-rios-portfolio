/**
 * Tech Belt content (UX Spec, Section 3). Kept intentionally flat — this is
 * a recognition strip, not a ranked list, so every item gets equal visual
 * weight in the component that renders it.
 */
export const techStack: string[] = [
  "SQL",
  "Python",
  "Power BI",
  "Excel",
  "Pandas",
  "Git",
];
