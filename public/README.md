# Static assets

- **`resume.pdf`** — done. The real file is in place; the nav and hero download buttons resolve correctly.
- **`images/projects/`** — done. All 12 real dashboard screenshots are in place, referenced from `src/data/projects.ts`.

Favicon is handled in code via `src/app/icon.svg` — no file needed here for that.
